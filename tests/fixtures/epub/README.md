# Generated EPUB fixtures

The integration tests build tiny EPUB 3 archives from original, project-authored strings in
`generate-fixtures.rs`. No third-party book content is stored here. ZIP timestamps and entry order
are fixed, so identical source inputs produce byte-identical fixtures.
