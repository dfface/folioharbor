# 实施计划

本目录保存从已批准设计规格生成的可执行实施计划。计划必须遵守 TDD、任务级评审、独立提交和完成前验证。

## 当前计划

- [第一条 EPUB 端到端纵切实施计划](2026-08-05-first-epub-vertical-slice.md) — 23 个按依赖排序的任务，等待在独立实现 session 中执行。

实施前应使用 Superpowers `using-git-worktrees` 创建隔离工作树，再选择 `subagent-driven-development`（推荐）或 `executing-plans`。
